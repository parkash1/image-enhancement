import React from "react";
import "./Header.css";  // Importing the CSS file from the same folder

function Header() {
    return (
        <header>
            <h1>Welcome to the Image Enhancement App</h1>
        </header>
    );
}

export default Header;
